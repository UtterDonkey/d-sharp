let sequence = [1, 2, 3, 4, 5];
declare nextValue = function(){
    const s = new Sequence(...sequence);
    return s.forecastNext();
}
console.log(nextValue);
sequence = [1, 2, 4, 8, 16];
console.log(nextValue);